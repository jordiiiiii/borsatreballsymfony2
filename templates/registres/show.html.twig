{% extends 'base.html.twig' %}

{% block title %}Registres{% endblock %}

{% block body %}
    <h1>Registres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ registre.id }}</td>
            </tr>
            <tr>
                <th>Correu</th>
                <td>{{ registre.correu }}</td>
            </tr>
            <tr>
                <th>Ip</th>
                <td>{{ registre.ip }}</td>
            </tr>
            <tr>
                <th>Data</th>
                <td>{{ registre.data }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ registre.nom }}</td>
            </tr>
            <tr>
                <th>Cognom1</th>
                <td>{{ registre.cognom1 }}</td>
            </tr>
            <tr>
                <th>Cognom2</th>
                <td>{{ registre.cognom2 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('registres_index') }}">back to list</a>

    <a href="{{ path('registres_edit', {'id': registre.id}) }}">edit</a>

    {{ include('registres/_delete_form.html.twig') }}
{% endblock %}
